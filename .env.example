# =============================================================================
# Root .env — dibaca oleh docker-compose.yml
# Salin file ini ke .env dan isi nilainya. Jangan commit .env ke git.
# =============================================================================

# PostgreSQL (untuk docker-compose)
POSTGRES_USER=sugity
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=sugity-production-planning
